// Shared between the reminder banner (nudges the staff member themselves)
// and the admin shift log (flags it for a manager) -- kept in one place so
// the two don't drift to different thresholds.
export const LONG_SHIFT_HOURS = 10;
